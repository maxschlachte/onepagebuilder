## 1. Restructure chapter data

- [x] 1.1 In `src/data/factions/space-marine-chapters.ts`, split the 19 units and their upgrade groups into four bundles (Blood Angels, Dark Angels, Grey Knights, Space Wolves) by their existing flavor attribution, each shaped as `{ units: UnitProfile[], upgradeGroups: UpgradeGroup[], armyRules: SpecialRule[] }` (built directly via `unit()`/`group()`, since none of these units have cross-section prerequisites requiring `faction()`'s label-resolution pass).
- [x] 1.2 Change every chapter unit's construction to use `factionId: 'space-marines'` (via `unit('space-marines', ...)` or equivalent) so chapter unit ids resolve as `space-marines.<slug>`.
- [x] 1.3 Re-namespace each chapter's upgrade-group ids (currently `A`–`L`) to chapter-prefixed ids (e.g. `ba-a`, `da-e`, `gk-h`, `sw-k`) so they don't collide with base Space Marines' `A`–`O` groups or another chapter's groups; update each chapter unit's `upgrades` string to reference the new ids.
- [x] 1.4 Move the four chapters' `armyRules` (`Aegis`, `Blood Chalice`, `Brothers`, `Counter-Attack`, `Deathwing`, `Narthecium`, `Rage`, `Shroud of Angels`, `Tactics`, `Teleporter`, `Wolf`) to be exported alongside the per-chapter bundles (Rage duplicated in both Blood Angels' and Space Wolves' bundles, since Death Company and Wulfen both carry it); dedup against `space-marines.ts`'s existing `Narthecium`/`Tactics` entries happens at assembly time in `chapters.ts`. Dropped the four unwired `'... Chapter'` prose-only `armyRule` entries (superseded by real Chapter Tactics options).
- [x] 1.5 Remove the `faction({...})`/`spaceMarineChapters` top-level export and its registration in `src/data/index.ts`'s `rulesDatabase.factions` (16 → 15 factions).

## 2. Chapter assembly module

- [x] 2.1 Add `isVehicle(profile)` and `isHero(profile)` one-line predicate siblings next to `isInfantry` in `src/domain/calc.ts` (checking for the `Vehicle`/`Hero` rule id respectively), exported for reuse.
- [x] 2.2 Create `src/data/chapters.ts` exporting a `ChapterId` union (`'blood-angels' | 'dark-angels' | 'grey-knights' | 'space-wolves'`) and a small metadata list (id, display name) for the chapter selector UI.
- [x] 2.3 In `chapters.ts`, define each chapter's Chapter Tactics table as `{ appliesTo: (unit: UnitProfile) => boolean, label: string, cost: number, rule: string }[]`, matching the seven modifiers: Blood Angels (Infantry→Furious +10, Vehicle→Fast +5), Dark Angels (Terminators→Deathwing +20, Bike Squad→Scout +10, Attack Bike→Scout +5), Grey Knights (Infantry→Aegis +5, Vehicle→Aegis +5), Space Wolves (Hero→Wolf +30, Infantry→Counter-Attack +10).
- [x] 2.4 Implement `getEffectiveFaction(factionId: string, chapterId?: string): Faction | undefined`: passthrough to `getFaction(factionId)` unless `factionId === 'space-marines' && chapterId`; otherwise assemble a merged `Faction` — base `spaceMarines.units` plus the chapter's units, base `upgradeGroups` plus the chapter's re-namespaced groups plus a synthesized per-unit `'<chapter>-tactics-<unit-slug>'` group built via the existing `group()`/`section()` helpers, and base `armyRules` plus the chapter's armyRules deduped by id.
- [x] 2.5 In the assembly step, for every unit (base + chapter) matching a Chapter Tactics row's `appliesTo`, append a synthesized per-unit group id to a cloned copy of that unit's `upgradeGroups` (never mutate the base `spaceMarines`/chapter-bundle objects in place). Note: implemented as one synthesized group *per matching unit* (containing only that unit's applicable rows), not one shared group per chapter, so a Vehicle never sees an Infantry-only option like Furious.
- [x] 2.6 Added `src/data/chapters.test.ts` (9 tests, all passing): no-chapter/unknown-chapter/non-Space-Marines passthrough returns the identical base faction object; each chapter's assembled faction includes its extra units with no cross-chapter leakage; category-wide options (Furious/Fast/Aegis/Counter-Attack/Wolf) appear only on matching-category units; named-unit options (Deathwing) appear only on that unit; base faction singleton is never mutated.

## 3. Domain/store wiring

- [x] 3.1 Add `chapterId?: string` to `ArmyList` in `src/domain/list.ts`; bump `LIST_SCHEMA_VERSION` from 1 to 2. (Kept as plain `string`, not `ChapterId`, to avoid `domain/` depending on `data/` — validated against the `ChapterId` union at the store/data layer, matching how `factionId` is also a plain string.)
- [x] 3.2 Update `createList(name, factionId, pointsCap, chapterId?)` in `src/stores/lists.ts` to accept and store the optional chapter (only kept when `factionId === 'space-marines'`).
- [x] 3.3 Replace every list-scoped `getFaction(list.factionId)` call in `src/stores/lists.ts` (`sanitizeLinks`, `groupIsValid`, `combineUnits`, `attachToUnit`, `joinGroup`, `toggleUpgrade`) with `getEffectiveFaction(list.factionId, list.chapterId)`. Also replaced every `getUnit(list.factionId, ...)` call in those same functions with a new `findUnitInFaction(faction, unitId)` helper that looks up within the already-resolved (possibly chapter-assembled) `Faction` object — the raw `getUnit()` only searches the base faction's static unit list, which would silently fail to resolve chapter units.
- [x] 3.4 Update `validateImported` to accept an optional `chapterId` field: reject if it names an unknown chapter or is present while `factionId !== 'space-marines'`; resolve unit/upgrade ids against `getEffectiveFaction(factionId, chapterId)` instead of the raw faction.
- [x] 3.5 `exportListToJson`/`downloadList` needed no changes — they serialize the whole `ArmyList` object, which now includes `chapterId` automatically whenever present.
- [x] 3.6 Added `src/data/legacy-chapter-migration.ts` (`remapLegacyChapterList`) and wired it into `load()` (`.map(remapLegacyChapterList).map(sanitizeLinks)`): for any parsed list with `factionId === 'space-marine-chapters'`, looks up each unit's slug against a static legacy-slug→chapter table, infers `chapterId` as the majority chapter (ties broken by `chapters` array order), rewrites `factionId` to `'space-marines'`, remaps surviving units' `unitId`s and `selectedUpgrades` group-letter prefixes to the new namespaced ids, and drops (with `console.warn`) any unit belonging to a different chapter than the inferred one.
- [x] 3.7 Added tests: `src/stores/lists.test.ts` ("Space Marine chapter selection" — `createList` round-trips a chapter, ignores it for non-Space-Marines factions, chapter units are addable via `getEffectiveFaction`, `validateImported` accepts/rejects chapter data); `src/data/legacy-chapter-migration.test.ts` (4 tests — passthrough for non-legacy lists, chapter inference, id remapping, dropping a mismatched-chapter unit). All passing (197/197 total).

## 4. Builder/print view wiring

- [x] 4.1 In `src/views/BuilderView.vue`, changed the `faction` computed to call `getEffectiveFaction(list.value.factionId, list.value.chapterId)` instead of `getFaction(list.value.factionId)`.
- [x] 4.2 In `src/views/PrintView.vue`, applied the same `getEffectiveFaction` substitution — it was the only place reading the list's faction.
- [x] 4.3 Confirmed by inspection: the roster/"Add unit" list and Details panel (`RosterUnitPreview.vue` etc.) all read from the `faction` computed's `.units`/`.upgradeGroups`, with no separate faction lookup — they pick up chapter units and Chapter Tactics options automatically once `faction` resolves to the assembled faction. No roster-specific code changes needed. Full suite (197 tests) and typecheck still pass.

## 5. Create Army List dialog

- [x] 5.1 In `src/components/CreateListDialog.vue`, added a chapter `<select>` (`None` + the four chapters, passed in via a new `chapters` prop) shown only when the selected faction is `'space-marines'`, defaulting to `None`.
- [x] 5.2 Reset the chapter selection to `None` (`''`) via a `watch(factionId, ...)` when the faction selection changes away from Space Marines.
- [x] 5.3 Included `chapterId` (undefined when None or non-Space-Marines) in the dialog's `create` event payload.
- [x] 5.4 In `src/views/ListsView.vue`'s `onCreate`, passed the chapter through to `store.createList(...)`; also passed `store.chapters` into `<CreateListDialog>`.

## 6. List display

- [x] 6.1 In `src/views/ListsView.vue`'s list-row summary, added a `chapterName()` helper and show the chosen chapter alongside the faction name for chapter lists, e.g. "Space Marines (Blood Angels)". Also fixed `listTotal()` to resolve the chapter-aware effective faction (`getEffectiveFaction`) instead of the raw faction — the raw lookup would have silently undercounted cost for lists containing chapter units or chapter-tactics selections.

## 7. Tests and verification

- [x] 7.1 Updated `src/data/index.test.ts`'s faction-count assertion from 16 to 15; the file's generic per-faction integrity checks (unique ids, resolvable rule references, upgrade-group letter resolution, etc.) pass unchanged against the restructured data — no other edits needed there.
- [x] 7.2 Added a "Space Marine chapter specialization" describe block to `src/views/integration.test.ts` (3 tests): roster shows chapter units alongside base units and a chapter unit can be added and built; a Chapter Tactics option (Furious) appears on an eligible unit and updates the live total when selected; a chapter-less Space Marines list shows no chapter units or Chapter Tactics options.
- [x] 7.3 `npm run build` (typecheck + vite build) and `npm run test` both pass clean: 200/200 tests, 16 test files, no typecheck errors.
- [x] 7.4 Manually verified against the running dev app (headless Chromium, screenshots): Create Army List dialog shows the Chapter selector for Space Marines with "Blood Angels" selectable; the created list's roster shows chapter units (Sanguinary Priest, Death Company, Sanguinary Guard, Furioso Dreadnought, Baal Predator) alongside all base Space Marines units; adding Tactical Marines shows a "Chapter Tactics → Furious +10pts" checkbox that, once checked, updates the live total from 120→130/750pts; the Lists view summary shows "Space Marines (Blood Angels) · 130/750pts"; a plain (no-chapter) Space Marines list shows no chapter selector interaction artifacts and no Sanguinary Priest/Furious in its roster. No console/page errors observed.
