No capability spec changes in this proposal.

This is a pure internal-refactor change (extracting five inline badge `<span>` elements in `BuilderView.vue` into a shared `Badge.vue` component) with no spec-visible behavior or requirement changes — see proposal.md's Capabilities section. Nothing here is archived into `openspec/specs/`.
